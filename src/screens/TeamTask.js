import React from "react";
import { Text, View, StyleSheet } from "react-native";

const TeamTask = () => {
  return <Text>Team Task</Text>
}

const styles = StyleSheet.create({});

export default TeamTask;
